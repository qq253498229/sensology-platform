eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 17777
spring:
  application:
    name: zuul-server
zuul:
  routes:
    api-a:
      path: /api-a/**
      serviceId: eureka-client


